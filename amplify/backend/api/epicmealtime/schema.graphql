type Entity @model {
	id: ID!
	description: String!
}

type User @model {
	id: ID!
	name: String!
}

type UserEntity @model @searchable {
	id: ID!
	userId: String!
	entityId: String!
	result: Boolean!
}
